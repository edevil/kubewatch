apiVersion: v1
kind: Pod
metadata:
  name: kubewatch
spec:
  containers:
  - image: edevil/kubewatch:v0.0.6
    imagePullPolicy: Always
    name: kubewatch
    volumeMounts:
    - name: config-volume
      mountPath: /root
  restartPolicy: Always
  volumes:
  - name: config-volume
    configMap:
      name: kubewatch